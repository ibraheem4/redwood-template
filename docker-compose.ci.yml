version: '3.8'
services:
  api:
    build:
      context: .
      target: api
      dockerfile: Dockerfile.dev
    environment:
      REDWOOD_API_URL: ${REDWOOD_API_URL}
      DATABASE_URL: ${DATABASE_URL}
      TEST_DATABASE_URL: ${TEST_DATABASE_URL}
    depends_on:
      - test_db
    volumes:
      - template_api_data_ci:/var/www

  test_db:
    image: postgres:15
    environment:
      POSTGRES_DB: ${POSTGRES_DB_TEST}
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
    volumes:
      - template_test_db_data_ci:/var/lib/postgresql/data

volumes:
  template_api_data_ci:
  template_test_db_data_ci:
